using NUnit.Framework;

namespace Mirabeau.uTransporter.UnitTests.Generators
{
    [TestFixture]
    public class TabGeneratorTests
    {
        [Test]
        public void Method_ShouldDo_ShouldReturn()
        {
            //IFileWriter fileWriter = MockRepository.GenerateStub<IFileWriter>();
            //IContentReadRepository contentReadRepository = MockRepository.GenerateStub<IContentReadRepository>();

            //contentReadRepository.Expect(m => m.GetAllContentTypes()).Repeat.Once().Return();

            //TabGenerator tabGenerator = new TabGenerator(fileWriter, contentReadRepository);
            //tabGenerator.Generate();
        }
    }
}
